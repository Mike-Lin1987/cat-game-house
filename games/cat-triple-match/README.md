# 貓咪三層配對

純 HTML、CSS 與傳統 JavaScript 的完全離線三消配對遊戲。直接雙擊 `index.html` 即可遊玩，`review.html` 可逐關檢視固定資料與 Solver 證據。

- 只能選擇未被更高層卡牌遮住的卡牌。
- 卡牌依圖案集中進入 9 格暫存槽；每三張相同圖案同步消除。
- 同一步形成三消時，先消除再判定滿槽，因此第 9 張配對仍可繼續。
- 100 關皆為固定資料；Solver 不讀 `knownSolution`。
